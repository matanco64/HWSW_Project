[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pyflate-rs"
version = "0.1.0"
description = "Native bzip2 symbol-decode kernel for the pyperformance pyflate benchmark"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
