# mtf_cam — see hw/FLOW.md for targets. Placeholders until the RTL stage.
TOPLEVEL        := mtf_cam
VERILOG_SOURCES := $(wildcard rtl/*.sv)
MODULE          := test_mtf_cam
include ../common/Makefile.cocotb
