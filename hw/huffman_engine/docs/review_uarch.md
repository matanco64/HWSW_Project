# huffman_engine — uArch review

## Pass 1 (2026-09-08, agent)

Scope: `docs/uarch.md` vs `docs/prd.md` (F1..F16, K1..K4), `docs/mas.md` (§2/§4/§5/§8),
ADR-0003/0006/0008, `docs/decode_model.py` (re-run: output matches every §7 number),
`golden/canonical_model.py`, `golden/pyflate_ref.py` trace facts. Concurrency and complexity
focus. `must` = RTL built from this spec would be wrong, or a KPI derivation is unsound.

| # | Severity | Location | Finding |
|---|---|---|---|
| U1 | must | §2 mermaid + §4 | **Compare direction contradicts the priority rule — the decoder as specified returns the wrong symbol on every multi-length table.** §4 defines match as `window[19:20−l] ≥ first_code[l]`; §2 says "priority encode shortest match l". For a canonical code of true length L, every prefix with l ≤ L satisfies `prefix_l ≥ first_code[l] + count[l] ≥ first_code[l]` (prefix recurrence: `first_code[L] ≥ (first_code[l]+count[l]) << (L−l)`), so ALL lengths 1..L "match" and shortest-wins picks l = 1 (first_code[1] = 0 matches everything). Example {A='0', B='10', C='11'}: window `11…` → shortest match returns A, not C. PRD-F2's two-sided condition is only equivalent to a one-sided compare as either (a) `window-prefix < first_code[l] + count[l]` (= `first_code[l+1] >> 1`, left-alignable the same way), shortest match wins, no-match-anywhere = ERR_NOCODE — or (b) `≥ first_code[l]`, **longest** match wins, plus an explicit invalid-window detect. Fix: rewrite §2/§4 with form (a) (it also makes ERR_NOCODE = "no comparator asserts", decidable in C0) and note that `canonical_model.py::decode` (two-sided, count-0 skip) is the reference the chosen form must be proven equivalent to. |
| U2 | must | §2 pipeline, §3.1, §8 | **Every event keyed on the symbol VALUE is detected 2 cycles after C0 has moved on — EOB, ERR_SYMBOL, ERR_LIMIT, and the DEFLATE extra-bits trigger.** The symbol value exists only after the C1 symtab read; C0 free-runs at 1 sym/cycle, so when EOB (= ALPHABET−1 / 256) is recognised, C0 has already decoded 1–2 further "symbols" from post-EOB bits: BITS over-counts (breaks PRD-F4 exact-consumed-bits and F13's `next START_BIT = START_BIT + BITS`), sym_cnt may pop an extra selector at a 50-boundary, and post-EOB garbage can raise a spurious ERR_NOCODE/ERR_UNDERRUN before DRAIN is entered. Same for DEFLATE: a length/distance symbol must stall C0 for extra bits, but the sub-FSM only knows it is one at C1/C2 — by then C0 has consumed the extra bits as code bits. The uArch specifies neither early detection nor rewind. Fix: detect terminal/typed symbols in C0 — per table, latch at build time the (length, code-offset) of EOB (and, DEFLATE, the 257..285 / distance ranges are contiguous in symtab index space, so a C0 index-range compare works) — or specify an architectural rewind (BITS/occ/sym_cnt restore + pipeline squash) with its cost. |
| U3 | must | §5 memory table, §3.2 | **`count[]` is sized 20×9b — one array — but six per-table builds need six count sets.** Counts are folded at AXI-write time (before the doorbell), for all N_TABLES tables; PREFIX for table t needs `count[t][l]`. A single shared array would hold the sum over all tables and every build would be wrong. Fix: 6×20×9b (1.08 kbit) with write-time attribution per U4, and update the §5 total. |
| U4 | must | §3.2, ADR-0008 #4, MAS §4 LEN row | **Folded counts are not implementable against the MAS LEN layout without a software-ordering contract that is nowhere stated.** Entry e = t·ALPHABET + s: attributing a write to (table, length-bin) requires ALPHABET (and MODE, for the DEFLATE t=1-at-offset-ALPHABET rule) — but at write time these are *pending* registers software may write after the lengths, and an invocation that changes ALPHABET/N_TABLES without rewriting every stale LEN word leaves counts attributed under the old geometry. The decrement-old/increment-new rule only repairs words that are rewritten. Fix: either (a) state a binding driver/MAS rule — ALPHABET, MODE, N_TABLES written before any LEN word, and every word 0..⌈N_TABLES·ALPHABET/6⌉−1 of the new geometry rewritten each invocation, with a CLEAR_LEN mechanism (or doorbell-time zeroing) for shrinking configs — or (b) fold counts per raw word index with fixed ALPHABET_MAX stride (needs a MAS layout change), or (c) revert to a count pass (K2 = 2·ALPHABET+MAXLEN still meets the bound with zero margin — renegotiate ADR-0008 #4). Also state when counts reset: reset value 0 (§9) covers rst_n only, not invocation N → N+1. |
| U5 | must | §5 memory table | **Lengths window sized "288 × 5 b = 1.4 kbit" — 6× too small.** MAS §4: 288 *words* × 6 fields = 1,728 five-bit entries = 8.64 kbit. As written the array holds 1.7 of the 6 benchmark tables; the builder FILL pass reads len[s] per (table, symbol), so the full 1,728 entries must be stored. The "≈ 22 kbit total" and the K5-plausibility sentence need re-deriving (≈ 29.5 kbit with U3+U5 corrected — conclusion likely survives, the arithmetic doesn't). |
| U6 | must | §3.1, §3.3 | **`ERR_STARVED` and `ERR_LEN` are phantom flags — neither exists in the MAS §4 STATUS map or PRD-F9/F10 — and ERR_STARVED's definition contradicts PRD-F6/F10.** F6: `s_sel.tvalid` low *stalls* (never errors); F10: the error case is "TLAST already consumed when a selector is needed" = **ERR_SELECTOR**. §3.3 first says "tvalid low at pop → ERR_STARVED", then says the policy is stall — self-contradictory — and the "DEFLATE-mode mismatch" justification is void because `s_sel` is never popped in DEFLATE (tready = 0, MAS §5). §3.1's BUILD→ERR_S arc is labelled ERR_LEN; the MAS bit is ERR_TABLE. Fix: delete ERR_STARVED (tvalid-low = stall; TLAST-consumed = ERR_SELECTOR), rename ERR_LEN → ERR_TABLE. |
| U7 | must | §3.1, §8, §10 vs MAS §8 / PRD-F9/F10 | **Four MAS §8 / PRD-F10 rows have no uArch mechanism at all** (traceability holes; §10 waves at "§3.1 ERR_S arcs" which do not contain them): (1) build-time over-subscription ERR_TABLE — the count pass that computed Σ2^−l was removed by ADR-0008 #4; nothing says where Kraft overflow is now caught (natural home: PREFIX detects `first_code[l] > 2^l` via the 21-bit carry — say so, per table, with the §8-mandated side effects). (2) ERR_UNDERRUN — needs a last-valid-bit position (from TLAST beat + TKEEP low-run, TKEEP unmentioned in the §1 aligner row) compared against the consume pointer per symbol/extra-bit read, plus the START_BIT ≥ buffer-bits doorbell case. (3) ERR_SYMBOL — the ≥286 / ≥30 checks appear in no block. (4) ERR_LIMIT — no SYMBOL_LIMIT counter/comparator exists in any block description, and its "EOB as beat SYMBOL_LIMIT → DONE" tie-break (MAS 0x110) interacts with U2's late EOB detect. |
| U8 | must | §3.1 SKIP, §7 first-symbol claim vs MAS §5 | **SKIP only consumes `START_BIT mod 32` — the ⌊START_BIT/32⌋ whole-word discard is unspecified, and the §7 "first symbol ≤ 1,006 cycles" claim is unsound.** MAS §5: DMA presents the buffer from its first byte; the engine only needs START_BIT within it. Benchmark sym_start_bit ≈ 8.9 k bits → ≈ 278 words to discard, of which only 4 can prefetch during BUILD (MAS §8 doorbell row caps build-time prefetch at 4 beats), so ~274 cycles serialize between BUILD and DECODE: first symbol ≈ 1,28x, not 1,006 (the PRD acceptance bound N_TABLES×K2+16 = 1,900 still holds; the uArch's own number does not, and 149,275 / K4a 2.99 ms exclude these cycles although K1 is doorbell→DONE). decode_model.py has no SKIP term and seeds occ = 64 with the *pre-skip* prefetched words. Fix: specify the word-discard datapath (tready-limited, 1 beat/cycle, overlapping BUILD once the 4-beat cap lifts or with the cap raised), and re-derive the §7 first-symbol and cycle numbers. |
| U9 | must | §1/§5 aligner vs PRD-F4 / MAS §5 OVERFETCH ≤ 4 | **The 64b buffer + 4×32b prefetch FIFO can hold up to 191 unconsumed bits, i.e. 5–6 accepted beats wholly beyond the last consumed bit at EOB — violating OVERFETCH ≤ 4.** On the benchmark itself ~9 k bits follow EOB in the buffer, so a keep-everything-full refill policy reads back OVERFETCH = 5+ and `test_full_benchmark`/`test_corner` fail. No accept-stop rule is given ("window full" in MAS is undefined against this two-level structure). Fix: state the invariant the aligner enforces (e.g. beats accepted − beats fully consumed ≤ 4 ⇒ FIFO+buffer never hold > 128 unconsumed bits, which still covers MAXLEN=20 + 32b refill), and define exactly how OVERFETCH is counted at DONE. |
| U10 | should | §7 + decode_model.py:39-44 | **The "input beat dropped every 8 cycles: unchanged" sweep is vacuous — the stall knob is dead code on this workload.** `cycles % input_stall_every` is evaluated only inside the `while occ < ln` wait loop, which is never entered (occ starts at 64; line 48-49 refills 32b unconditionally every decode cycle, immune to the knob). Output is bit-identical with and without the knob (0 refill stalls both ways) — the claim "prefetch + 64b buffer absorb" is asserted, not simulated. A DMA gap must gate the *steady-state* refill (supply 28 b/cycle vs consume 3.585 mean / 20 max would in fact survive — but show it). Related model/uArch mismatch: the model gates decode on `occ ≥ ln`, the hardware (§8) on `occ ≥ MAXLEN` — the stricter hardware gate stalls where the model does not, so starvation behaviour is unvalidated in both directions. |
| U11 | should | §4 | The 21-bit left-aligned compare is under-specified: equivalence holds for `{1'b0, window[19:0]} ≥ (first_code[l] << (20−l))` (A ≥ b·2^k ⟺ A≫k ≥ b), but the equally natural reading `{window, 1'b0}` compares an (l+1)-bit window slice against first_code[l] and is wrong for odd first_code. State the exact window extension and that fc's low 20−l bits are zero. Also: prove (or check in RTL) that the winning comparator implies `code ≥ first_code[l]` so `code − first_code[l]` cannot underflow — with count[l] = 0 entries in the corrected U1 form a false match would index `symtab` with a wrapped 11-bit sum. And "`base[l]` UQ11.0 (≤ ALPHABET_MAX+? offsets, …)" contains a literal unresolved "+?" placeholder. |
| U12 | should | §1 huff_deflate, §4 | DEFLATE gaps: (1) nothing forces lengths 16..20 non-matching when MAXLEN = 15 — what do first_code[16..20] hold after a 15-length build, and how is the 15-bit reversed window presented to the 20-comparator fabric (top-aligned? which pad value)? (2) no block owns the lit↔dist table switch — cur_set belongs to huff_selector (bzip2); the per-event table-0/table-1 toggle for a copy event is unassigned. (3) extra bits are LSB-first integers of the *unreversed* stream, but the aligner's DEFLATE path is described only as reversing the peek window — the extra-bit extraction path (and its §6 timing) is unspecified. |
| U13 | should | §3.2, §6 AXI/regs row | Fold cost is real and unbudgeted: one LEN write = read old word, then up to 6 decrements + 6 increments landing on arbitrary count bins — a 12-input signed tally per bin (or wready stretched over 6 sub-cycles: say which). AXI-Lite partial `wstrb` makes it worse: 5-bit fields straddle byte lanes, so a partial write changes fields the strobes only half-cover — merge-with-old before diffing, or have the MAS ban partial strobes on 0x400+. "AXI/regs paths grape-proven < 10 ns" does not cover this logic; grape has nothing like it. |
| U14 | should | §3.1 DRAIN | DRAIN → DONE_S is conditioned only on "skid empty (TLAST handshaken)", but DONE requires the `s_sel` surplus drained to TLAST first (MAS §8 EOB row, PRD-F7); no arc or cycle cost for the drain, and the MAS rule "ABORT during the drain → DONE, drain stops" (MAS §8 ABORT row) has no corresponding DRAIN→DONE_S-on-abort arc — §3.1 only aborts from DECODE/BUILD. |
| U15 | should | §1 huff_tables, MAS 0x114/0x118 | DBG_DATA is "live (possibly partial) during a build" and readable while BUSY, but symtab has exactly 1R (decode) and 1W (build); a debug read during BUILD/DECODE contends with both. Specify the arbitration (e.g. DBG snoops only on idle port cycles and may return 0, or a dedicated shadow mux) — otherwise the RTL either stalls the C0 loop for a debug read (K1 jitter) or returns garbage. |
| U16 | should | §8 withdrawal row vs PRD-F10 | Skid flush on ERR drops *valid pre-error* symbols still un-handshaken (up to 2), so SYMBOLS (handshaken beats) reads N−1/N−2, while PRD-F10's acceptance is "SYMBOLS == symbols before the error". ADR-0006 makes the withdrawal intentional; the uArch should state the counting semantics (SYMBOLS = handshaken only; withdrawn beats are decoded-but-never-emitted) so the testplan checks the right number. |
| U17 | should | §5 doorbell path vs MAS §5 BRESP rule | MAS holds BRESP ≤ 8 cycles for the doorbell-time checks including "lengths > MAXLEN" — over 1,728 entries, against the *latched MODE's* MAXLEN (16..20 legal in bzip2, illegal in DEFLATE). No uArch mechanism: needs a write-time fold (sticky any-length-in-21..31 flag + per-bin counts 16..20 nonzero check for MODE=1 — the latter depends on U3/U4's per-table counts). Say where this lives. |
| U18 | nit | §7 K4a line | "2.99 ms ≤ 3.06 ms bound" — 3.06 ms is PRD K4a's own *estimate* (153,121-cycle stretch model), not a requirement bound; the binding number is K1's block ≤ 163,100 cycles = 3.26 ms. Also "N_TABLES × K2 + SKIP + 2 = 1,006" shows no SKIP value (1,002 + 2 + SKIP = 1,006 ⇒ SKIP = 2, unexplained; see U8). |
| U19 | nit | §3.2, §3.1 | "W1C-style rewrite" misuses W1C (it is a read-modify replace, nothing write-1-to-clear); "DECODE → ABORT_S: abort (& at BUILD boundary)" is a garbled arc label. |
| U20 | nit | §6 C0 row | The C0 path omits the cur_set 6:1 first_code read-mux (~3 levels ahead of the 20 compares) and the DEFLATE window bit-reverse mux; itemise them so the 13 ns estimate survives review at PPA. |
| U21 | nit | §10 | Traceability table has no rows for PRD-F14/F15/F16 (F16 → §9 exists in text), and no owner named for ERR_BUSY, BUILD_CYCLES, or OVERFETCH counters (huff_regs "counters" is implied — name them). |
| U22 | nit | §3.2 | Build-cost formula uses MAXLEN = 20 unconditionally; in DEFLATE mode PREFIX is 15 cycles (K2 per DEFLATE table = ALPHABET + 15). State the mode dependence. |

### Summary: 9 must / 8 should / 5 nit

Model check: `python3 hw/huffman_engine/docs/decode_model.py` re-run 2026-09-08; every §7
number reproduced (149,275 cy / 1.0068; build 1,002; 1.185; 1.2568; K2 167/314, 308/596;
first-symbol 1,002-based). The headline K1 derivation is sound *given* the model's assumptions;
U8 (SKIP cycles) and U10 (starvation sweep) are the assumptions that do not hold.

## Pass 2 (2026-09-08, agent)

Scope: revised `docs/uarch.md` + reworked `docs/decode_model.py` (re-run 2026-09-08: every §7
number reproduced — 149,276 cy / 1.0068, build 1,002, skip 276, 1 refill stall; sweeps 1.0068 /
1.0236 / 1.2568; K2 167/314, 308/596) vs `docs/mas.md` (approved), PRD. Each U# fix checked for
new errors; fresh contradiction scan.

### Verification of pass-1 musts

| # | Resolved | Evidence |
|---|---|---|
| U1 | **yes (bzip2)** — DEFLATE residual is N1 | Math checked: `la(fc[l]) = (fc[l−1]+cnt[l−1])<<1<<(MAXLEN−l) = (fc[l−1]+cnt[l−1])<<(MAXLEN−(l−1)) = limit_la[l−1]` — the §4 identity holds, so the per-l ranges `[limit_la[l−1], limit_la[l])` tile the window space and smallest-matching-l is exact. Example {1,2,2}: limit_la[1]=1<<19, limit_la[2]=(2+2)<<18=2^20; w='11…'≥2^19 fails l=1, matches l=2, code=w>>18=3, offset 3−first_code[2]=1 → C ✓ (pass-1 counter-example now decodes right). Empty lengths: cnt[l]=0 ⇒ limit_la[l]=limit_la[l−1], so if such l matches, l−1 matches too ⇒ the smallest match always has cnt>0 (induction down to limit_la with all-zero prefix = 0, unmatched). Edge w ≥ limit_la[MAXLEN]: complete trees give limit_la[MAXLEN]=2^20 > any 20-bit w (always a match); incomplete trees leave w ≥ limit_la[20] matching nothing ⇒ ERR_NOCODE — correct. 21-bit compare `{1'b0,w} < limit_la` handles the 2^20 limit ✓. |
| U2 | **partial** | C0 events (eob latch, no-match, limit, underrun) and DEFLATE II=2 resolve are specified and kill the phantom-decode/rewind problem in principle. But the fix itself has two new musts: the eob latch is never invalidated (N2) and the ERR_LIMIT counter semantics contradict MAS 0x110 (N3). |
| U3 | **yes** | §3.2/§5: `count[6][20]` 9b = 1.08 kbit ("1.1 kbit" row ✓); 9b holds max 288; PREFIX reads `count[t][l]` per table. Reset: §9 regs→0 zeroes counts *and* window together — mutually consistent; invocation N→N+1 needs no clear because counts mirror the persistent window. Doorbell REJECT (ERR_PARAM) writes nothing ⇒ counts stay consistent ✓. Back-to-back RMW: flop-based one-cycle update reads the previous cycle's registered word/bins, and the shared `axi_lite_if` cannot retire two writes in one cycle — no hazard. |
| U4 | **no** | The attribution mechanism is built on a MAS that does not exist: §3.2 says "table = (w − LEN_BASE) / LEN_STRIDE — the MAS window is per-table at fixed strides", but MAS §4 LEN row still says "packed continuously, table-major … entry e = t·ALPHABET + s at word ⌊e/6⌋" — no strides, table boundary depends on *pending* ALPHABET, and one 32-bit word can straddle two tables (ALPHABET=147: word 24 holds entries 144..149 = t0's 144..146 + t1's 0..2), so address-only attribution is impossible against the real layout. The cited "driver contract, MAS §6 note" is also phantom — MAS §6 has no zeroing-on-shrink text (grep: no "zero"/"shrink" hit in §6) — and the scheme contradicts MAS §4 bit-10: "entries beyond N_TABLES·ALPHABET … are ignored by both checks" (mirror-counts do NOT ignore them; the uArch instead requires them zero). See N4. |
| U5 | **yes** | 6×288×5b = 8,640 b ✓. §5 re-added: 17,280+2,520+2,400+1,536+1,080+8,640 = 33,456 b + aligner 128b + skid 66b ≈ 33.7 kbit ⇒ "≈ 34 kbit" ✓. eob latch 25b = 5+20 ✓; base row 6×(220+11+25)=1,536 ✓. |
| U6 | **yes** | ERR_STARVED/ERR_LEN gone; §3.1 error set = MAS bits 9..15 exactly (ERR_BUSY stays a regs-level flag, correctly outside the FSM); §3.3 "skid empty at pop = stall"; §8 "stall, never loss/error" — no self-contradiction left. |
| U7 | **yes** (two residual shoulds) | (1) Kraft: PREFIX checks `first_code[l]+count[t][l] > 2^l` → ERR_TABLE; since the check passes at l−1, fc[l] ≤ 2^l — no wrap before first detection ✓; PREP→ERR_S arc exists. (2) ERR_UNDERRUN: `last_valid_bit` from TLAST/tkeep in the §1 aligner row + §8 row + C0 consume compare ✓. (3) ERR_SYMBOL at the DEFLATE resolve ✓ mechanism named, but the valid-bit story is muddled (N6). (4) ERR_LIMIT: mechanism exists but see N3. Doorbell-time length>MAXLEN (U17, unclaimed) still has no mechanism — now entangled with the count bins (N9). |
| U8 | **yes** | PREP = BUILD ∥ SKIP, independent units (aligner vs builder); model implements `cycles = max(build, skip)` and simulates arrivals; benchmark skip 276 < build 1,002 ⇒ "fully hidden" ✓; first-symbol claim 1,004 ≪ acceptance bound N_TABLES×K2+16 = 1,900 ✓. Residuals: ceil/floor and "+2" arithmetic (N11, nit), and accepting ~277 discard beats during BUILD vs MAS "prefetch ≤ 4 beats" wording (N7, should). |
| U9 | **yes** | Capacity: 64b buffer + 2×32b FIFO = 128 b ⇒ at most 4 beats can sit wholly beyond the last consumed bit; `tready` drops on FIFO full ✓. Refill sufficiency proven: refill fires when occ ≤ 32; with FIFO non-empty, occ ∈ [20,64] is closed under a 20-bit consume (occ ≤ 52 → occ+12; occ > 52 → occ−20 ≥ 33) ⇒ zero steady-state stalls even at all-20b codes (model: 1 stall, 10,000 syms ✓). The benchmark's "1 refill stall" is the post-PREP initial window fill (model starts arrivals at PREP end — conservative; real HW can pre-fill during BUILD's tail); bounded and understood. |

Model (task B): all §7 quotes match the run. `tick(k)` delivers arrivals up to cycle k into a
capped FIFO and `refill()` moves ≤ 1 beat/cycle — each loop iteration advances `cycles` exactly
once before tick+refill, so no beat is double-counted. The tail gate (`remaining ≤ occ and
remaining < MAXLEN`) cannot deadlock: `remaining ≥ ln` always (it is the sum of remaining
lengths), and the model's beat supply is unbounded so `occ` always grows past the gate. That
unbounded supply is also the model's remaining fidelity gap (N8).

### New findings

| # | Severity | Location | Finding |
|---|---|---|---|
| N1 | must | §4 window w, §3.2 DEFLATE | **The DEFLATE window alignment as written breaks every DEFLATE compare.** §4: "DEFLATE: bit-reversed view of the low 15, upper 5 zero" puts the 15-bit code in w[14:0] — but limit_la is left-aligned at MAXLEN=20 (`<< (20−l)`), so e.g. l=1's limit sits at bit 19 while the code bit sits at bit 14: every compare is misaligned by 5. The consistent design is the opposite: code TOP-aligned in w[19:5], LOW 5 zero, MAXLEN=20 shifts everywhere — which also makes the claimed "lengths 16..20 non-match via zero counts" rule actually work (fc[16]=2^16 ⇒ limit_la[16..19]=2^20, matched but never smallest). The alternative reading (MAXLEN_eff=15 in the shift) leaves limit_la[16..20] with negative shift — undefined — and voids the zero-count rule. Fix: one sentence — top-aligned, low-5 zero pad, shifts stay MAXLEN=20. |
| N2 | must | §3.2 FILL, §2 C0 events | **The per-set (eob_len, eob_code) latch is never invalidated — stale latches fire false EOB.** FILL writes the latch only when s == ALPHABET−1 / 256 with len ≠ 0. The DEFLATE distance set (indices ALPHABET..ALPHABET+29, s ≤ 29) never latches, and any bzip2 table where EOB's len is 0 skips it — so the latch holds the *previous invocation's* (l, code), and a C0 equality hit on it mid-decode raises a spurious EOB (wrong DONE, truncated output). §9 reset covers rst_n only. Fix: clear eob_len := 0 for all six sets at PREP entry (decoded l ≥ 1 never equals 0, so len 0 = never-match), and state the latch write is len≠0-qualified. |
| N3 | must | §2 "SYMBOLS-issued counter", §8 withdrawal row vs MAS 0x110/§8 | **ERR_LIMIT on the C0 *issued* count contradicts MAS's "SYMBOL_LIMIT beats have been *emitted*".** Under m_sym backpressure C0 issue runs up to ~4 ahead of handshake (C1+C2+2-deep skid); when the issued counter hits SYMBOL_LIMIT, ERR_S flushes the skid (withdrawal, §8), so the in-flight beats never emit and final SYMBOLS reads SYMBOL_LIMIT−1..−4 — MAS says the error fires *after* LIMIT beats emitted. Also the MAS tie-break "EOB as beat SYMBOL_LIMIT → DONE" needs a same-cycle C0 priority (eob-match wins over limit) that §2 never states. Fix: stop *issuing* at LIMIT but let the pipeline/skid drain and raise ERR_LIMIT only after the LIMIT'th beat handshakes (no withdrawal on this error), and state eob ≻ limit priority in C0. |
| N4 | must | §3.2 vs MAS §4 LEN row + §6 | **U4's fix depends on three MAS provisions that do not exist** (see U4 row above): (a) per-table fixed-stride LEN layout — MAS is packed-continuous with ALPHABET-dependent, word-straddling table boundaries; (b) a HuffmanDriver zero-on-shrink contract — absent from MAS §6; (c) "entries ≥ ALPHABET must be 0" — MAS instead says entries beyond N_TABLES·ALPHABET are *ignored*. The stride layout fits the existing window exactly (LEN_STRIDE = 48 words, table t at 0x400+0xC0·t, 288 words total unchanged) but changes the software-visible pack rule (e = t·288+s, not t·ALPHABET+s), the DEFLATE distance mapping, and `load_lengths` — an approved-MAS amendment + driver change that must be written, not implied. The wstrb≠0xF ignored-whole rule is likewise a software-visible register-map semantic that belongs in the MAS. |
| N5 | must | §3.1 DRAIN→ABORT_S vs MAS §8 ABORT row | **The new abort-during-drain arc goes to the wrong state for half of DRAIN.** MAS §8: "if the EOB beat was already handshaken (i.e. during the s_sel drain) the drain stops and DONE is set instead" — but §3.1's single arc `DRAIN → ABORT_S: abort while draining` sets ABORTED unconditionally. DRAIN spans both phases (skid-to-TLAST-handshake, then s_sel drain); the arc must split: abort before the EOB handshake → ABORT_S (beat withdrawn, no TLAST), after it → DONE_S with the drain stopped. |
| N6 | should | §2 resolve vs §3.2 FILL | The symtab valid bit has no write rule that matches its read use: §2 says the resolve flags "invalid 286–287 (symtab valid bit)", but FILL writes `{valid, s}` for every len≠0 symbol — nothing sets valid=0 for 286/287, and an in-range decode (code < first_code+count) can never hit an unfilled slot anyway, so as written the bit is either always-1 or dead. Either classify by value range at resolve (s ≥ 286 / dist s ≥ 30) and drop the bit, or state FILL writes valid=0 for those indices. |
| N7 | should | §3.1 PREP vs MAS §8 doorbell row | SKIP ∥ BUILD accepts ~⌈START_BIT/32⌉ (benchmark: 277) `s_bits` beats during the build, while MAS §8 says "prefetch ≤ 4 beats" at the doorbell. Consistent only under the reading "≤ 4 accepted-but-unconsumed" (discarded skip words are consumed); the uArch should state that reading (and the testplan check it) explicitly, or the MAS row gets misread as a hard 4-beat acceptance cap that would serialize SKIP after BUILD (pass-1 U8's original reading). |
| N8 | should | decode_model.py `simulate()` | The cadence sweeps deliver only *code* beats: `next_beat = max(build, skip)+1` and `skip = start_bit//32` is charged at 1 cy/word regardless of `beat_every`, but real skip words arrive on the same throttled stream — at beat_every=5 the 277 skip beats take ≈1,385 cy (> build 1,002; ~383 cy exposed), at beat_every=8 ≈2,216 cy (~1,214 exposed). Corrected K1 ≈ 1.009 / 1.032 — both still ≤ 1.1, so the conclusion survives, but §7's "at 6.4 b/cycle: 1.0068 — unchanged" is an artifact. Gate the PREP skip rate on beat arrival too. |
| N9 | should | §3.2 RMW vs MAS 0x008/§5 BRESP rule (pass-1 U17, still open) | Length fields are 5b (0..31) but the bins are `count[6][20]`: values 21..31 (and the l=0 non-bin) have no stated RMW behavior, and the MAS doorbell-time "length > MAXLEN of the mode" rejection (BRESP ≤ 8 cycles over 1,728 entries) still has no uArch mechanism. Natural fix in the new scheme: per-table sticky any-length>20 flag maintained by the same RMW, plus the 16..20 bins nonzero check for MODE=1 — say where it lives. |
| N10 | nit | §4 / §3.2 first_code width | first_code sets are 20b but the recurrence reaches 2^20 (21b) once the tree is exhausted below MAXLEN (e.g. any complete DEFLATE tree at l=16..20, or bzip2 max-length < 20): fc wraps to 0 and limit_la[l] computes 0 (never-match) instead of limit_la[l−1] — harmless (a smaller l always matches every w first, proven above) but it silently breaks the §4 identity at those l; either widen fc to 21b or note the wrap-is-benign argument. |
| N11 | nit | §3.1/§7 first-symbol arithmetic | §3.1 uses ⌈START_BIT/32⌉ = 277, the model floors (276, "skip 276"); and "+2" does not cover the post-PREP refill (+1) plus the C0/C1/C2 depth to the first handshaken m_sym beat (~+4). First beat ≈ 1,007, still ≪ the 1,900 acceptance bound — fix the constant, not the design. |
| N12 | nit | §2 C0-events paragraph | "BITS counts exactly the consumed *code* bits" — PRD-F4/F13 need BITS to include DEFLATE extra bits too (huff_deflate consumes them); §1/§8 imply it, this sentence contradicts it. |
| N13 | should | §5 symtab row vs MAS 0x118 | "DBG_DATA reads only while idle — no contention" conflicts with the approved MAS: DBG_DATA is readable while BUSY and "live (possibly partial) during a build". If "idle" means idle *port cycles* (pass-1 U15's suggestion), say so and define what a colliding read returns; if it means module-idle, the MAS row must be amended. |

### Summary: 5 must open

U1/U3/U5/U6/U7/U8/U9 land (U1's core math and U9's refill invariant verified independently);
U2 lands mechanically but its fix introduces N2/N3; U4 is not resolved (N4). Open musts:
N1 (DEFLATE window alignment), N2 (stale eob latch), N3 (ERR_LIMIT issued-vs-emitted + EOB
tie-break), N4 (LEN stride layout / zeroing contract vs approved MAS), N5 (DRAIN abort arc vs
MAS DONE rule). Model reproduces every §7 number; N8 is its one remaining fidelity gap.

## Pass 3 (2026-09-08, agent — verification)

Scope: the pass-2 open musts (N1..N5) + the model fix (N8) against the revised `docs/uarch.md`,
the 2026-09-08 MAS amendment, and a re-run of `decode_model.py`; fresh contradiction scan of the
edited sections only.

| Finding | Resolved | Evidence |
|---|---|---|
| N1 (DEFLATE window alignment) | **yes** | §4: "bit-reversed 15-bit peek occupies w[19:5] — top-aligned, low 5 bits zero", shifts stay MAXLEN=20. Checked: `w < (fc+cnt)<<(20−l) ⟺ w>>(20−l) < fc+cnt`, and an l ≤ 15 code top-aligned occupies exactly w[19:20−l], so `code_l = w >> (MAXLEN−l)` extracts the code bits with lookahead below — identical to bzip2. Zero counts at 16..20 give limit_la[l]=limit_la[l−1] ⇒ matched-but-never-smallest (complete tree) or implies-l−1-matches (incomplete ⇒ ERR_NOCODE path intact). §3.2's MAXLEN_eff=15/zero-count sentence agrees. N10's fc-wrap-at-2^20 nit still applies at l=20 but is benign per the pass-2 argument. |
| N2 (stale eob latch) | **yes** | §3.2: all six `eob_len := 0` at PREP entry; latch write sits inside the `len[t][s] = l ≠ 0` FILL clause (len-0 EOB never latches). Covers both cases: stale set from invocation N−1 → cleared before any BUILD; DEFLATE distance set (s ≤ 29, never == 256) → never latches, stays 0. Decoded l is 1..20, so eob_len 0 can never equality-match in C0. Sets beyond N_TABLES are also cleared ("all six") and unreachable anyway (ERR_SELECTOR). |
| N3 (ERR_LIMIT emitted semantics + EOB tie) | **no** (narrowed) | The EOB-wins tie in §2 matches MAS 0x110's "(EOB as beat number SYMBOL_LIMIT → DONE)" ✓, and stop-issuing-at-LIMIT + drain would give exactly LIMIT emitted beats ✓ — but only "absent error-withdrawal", and §8's withdrawal row still says "skid flush on ERR/ABORT/doorbell" with no ERR_LIMIT exemption, while §3.1 keeps a direct DECODE→ERR_S arc with no raise-after-drain timing. RTL built from §8/§3.1 as written flushes the ≤ 4 in-flight beats and SYMBOLS reads < SYMBOL_LIMIT — pass-2's original defect. One sentence closes it: ERR_LIMIT is raised (and ERR_S entered) only after the LIMIT'th beat handshakes; the §8 flush excludes ERR_LIMIT (skid is empty by then). |
| N4 (MAS stride amendment) | **yes** | MAS "Amendments" 2026-09-08 bullet exists: LEN_BASE + 48·t strides, 48 words × 6 fields = 288 = ALPHABET_MAX per table, 6 × 48 = 288 words — exactly the existing 0x400 + 4·w, w = 0..287 window (table t at 0x400 + 0xC0·t; 0x880+ stays unmapped); fields ≥ ALPHABET written 0 with the `decode_block()` zero-on-shrink contract; wstrb ≠ 4'hF ignored whole; plus the prefetch-cap scoping (closes N7) and DBG_DATA FILL rule (closes N13). uArch §3.2 cites "MAS amendment 2026-09-08" for both the stride and the driver contract — no phantom reference remains, and §8's overfetch row carries the cap scoping. Residual (should, MAS wording): the §4 LEN row body and bit-10's "entries beyond N_TABLES·ALPHABET … ignored" clause still speak packed-continuous geometry the amendment supersedes, and the DEFLATE distance mapping under strides (table 1 at word 48) is implied, not restated. |
| N5 (DRAIN abort disposition) | **yes** | §3.1 splits the arc: `DRAIN → ABORT_S: abort before the EOB beat handshake` / `DRAIN → DONE_S: abort after it (MAS §8 — DONE wins)` — matches MAS §8's ABORT row ("if the EOB beat was already handshaken … the drain stops and DONE is set instead"); the s_sel-drain stop is implied by leaving DRAIN, remaining beats to the DMA flush per MAS. Normal DRAIN→DONE_S still requires TLAST handshaken + s_sel drained = MAS's DONE definition. |
| N8 (model skip cadence) | **yes** | Re-run 2026-09-08: `cycles = max(build, skip·beat_every)`, `next_beat = skip·beat_every + 1` throttle the skip words on the same stream. Output: benchmark 149,276 cy / 1.0068 (build 1,002, skip 276, 1 refill stall); beat_every=5 → **1.0093**, beat_every=8 → **1.0317**; backpressure 1.2568; K2 167/314, 308/596 — §7 quotes every number verbatim, and K1 stays ≤ 1.1 in all input-cadence sweeps. |

### New findings

| # | Severity | Location | Finding |
|---|---|---|---|
| N14 | must | §3.2 invalid bin vs MAS 0x00C bit 10 / §8 | **The new doorbell-time length check raises the wrong error bit.** §3.2: "a field > MAXLEN increments the per-table invalid bin — nonzero at the doorbell → **ERR_PARAM**". MAS assigns length > MAXLEN to **ERR_TABLE** in two places (bit-10: "length > MAXLEN of the mode (checked at the doorbell, rejection)"; §8 doorbell-rejection row: "ERR_PARAM, **or ERR_TABLE for a length > MAXLEN**"), and the driver's `load_lengths(validate=False)` corner exists precisely "to inject ERR_TABLE". One-word fix (→ ERR_TABLE), but as written the RTL and the testplan check different sticky bits. Related, still open from N9 (should): in DEFLATE the illegal range is 16..20, which lands in *valid* bins — the MODE=1 doorbell check (bins 16..20 nonzero → ERR_TABLE) is still nowhere stated; and the 6 × 9b invalid bins are absent from §5's count-bin sizing row (nit). |

No other new contradiction found in the edited sections: §3.2's stride/zeroing/wstrb text, §5's
DBG_DATA note, §8's overfetch-cap scoping and §7's numbers are mutually consistent and consistent
with the amended MAS.

### Summary: 2 must open

N1/N2/N4/N5/N8 land. Open musts: **N3** (ERR_LIMIT raise-after-drain timing unstated; §8
withdrawal row still flushes on every ERR — needs the ERR_LIMIT exemption sentence) and **N14**
(doorbell length-check wired to ERR_PARAM where the MAS mandates ERR_TABLE). Both are localized
one-sentence fixes; no re-architecture implied.

## Resolutions after pass 3 (author)

- N3: ERR_LIMIT is a drain-style stop — sticky raised at the LIMIT'th handshake, exempt from
  the withdrawal flush (§2 + §8); SYMBOLS lands exactly at LIMIT. Resolved.
- N14: invalid-length bin (and DEFLATE bins 16..20 of a used table) → ERR_TABLE at the
  doorbell, per MAS bit 10. Resolved.
- MAS-wording residuals (stale packed-continuous LEN row text) noted for the next MAS pass;
  the dated amendment is authoritative.

### Summary: 0 must open (3 passes, 9+13+2 findings)
