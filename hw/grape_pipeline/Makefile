# grape_pipeline — see hw/FLOW.md for targets. Placeholders until the RTL stage.
TOPLEVEL        := grape_pipeline
VERILOG_SOURCES := $(wildcard rtl/*.sv)
MODULE          := test_grape_pipeline
include ../common/Makefile.cocotb
