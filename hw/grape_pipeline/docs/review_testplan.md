# grape_pipeline — testplan review

## Pass 1 (2026-09-05, agent)

| # | Severity (must/should/nit) | Location | Finding |
|---|---|---|---|
| 1 | must | §2 F-04/F-05, §4 tolerance policy | The testplan claims the PRD-F4/F5 deviations are "expected to be exactly 0" ("expect 0: state is bit-exact"). False: hardware is bit-exact to `emulation.py`, but PRD-F4/F5 compare against the **golden model** (`nbody_ref` → libm `pow`), which differs from the emulation model — `golden/calibrate.py` header: measured maxima 1.06e-10 (r), 2.64e-12 (v), 1.7e-14 (energy), bounds are "~19x"/"~50x the measured maxima", i.e. deliberately nonzero. As written the checker either asserts ΔE==0 vs `nbody_ref` and spuriously fails sign-off, or (if "golden_final" secretly means the emulation state) never runs `nbody_ref.advance` at all and leaves PRD-F4/F5 unverified as stated. The checker must run `nbody_ref.advance(0.01, 20000)` separately and compare within the PRD bounds, expecting the calibrated nonzero values. |
| 2 | must | §2 F-10 checker + `cg_read_while_busy` | The checker "read value == committed snapshot held by scoreboard" is applied to {BODY, CYCLES, STEPS_DONE}. MAS §4 says "STATUS and the counters are **live**" (STEPS_DONE at 0x048: "live"), and CYCLES while BUSY has "**no atomicity guarantee**" (MAS §4, LO/HI note). PRD-F10 covers the state window only. A committed-snapshot equality check on live counters fails correct RTL (or forces broken RTL to pass review). Counters while BUSY need a monotonicity/bounds check, not snapshot equality. |
| 3 | must | §2 F-13 checker, §4 golden interface, F-33 | "flags == `emulation.advance` returned flag set" is ill-defined across invocations: FP_* are **sticky W1C** and "an accepted doorbell does not clear them" (PRD-F12, MAS §8), while `advance()` returns the per-call flag set. In `test_random` back-to-back invocations and F-33 chained runs, STATUS holds the union of all uncleared runs' flags and the compare is wrong unless the scoreboard's W1C policy (clear-before-each-doorbell, or accumulate expected flags across the chain) is defined. Neither §2 nor §4 defines one; `seq_w1c` exists but no test/sequence is required to issue it between runs. |
| 4 | must | §2 F-01/F-06/F-32 (`cg_cfg` NPAIRS bins) | NPAIRS = 0 is verified nowhere: `cg_cfg` NPAIRS bins are {1..10} (F-01 cross, F-32 "bins 1..10 each") and no test row programs NPAIRS=0. PRD-F6 requires "NPAIRS ∈ [0,10] accepted" and PRD §7 `test_corner` explicitly lists "NPAIRS = 0". A zero-pair step is the integrate-only corner (positions still advance by dt·v with no force ops); the plan as written signs off without ever executing it. |
| 5 | must | §2 (no row), §1 vs PRD §7 `test_corner` | PRD §7 `test_corner` requires "NSTEPS = 2³²−1 aborted after a few steps (counter width)". No matrix row, test, or bin covers it: the largest NSTEPS anywhere in the plan is 20000 (< 2¹⁶), so an NSTEPS register truncated to 16 bits — or a STEPS_DONE/CYCLES width bug — passes the entire testplan. PRD-F14's "cannot overflow" claim also rests on the full 32-bit NSTEPS path. |
| 6 | must | §2 F-11/F-21 checkers, `cg_abort` bins | The abort-lands-on-the-final-boundary case is both untested and mis-checked. PRD-F11: "If the boundary reached is step NSTEPS, the invocation completes normally: DONE asserts and ABORTED does not"; MAS §8 ABORT row: "DONE instead if the boundary reached is step NSTEPS"; RTL agrees (`rtl/grape_step_fsm.sv:82-85`, `abort_pend && (steps+1) != nsteps` → S_ABORT, comment "F11: DONE wins on the final step"). But F-11's checker unconditionally requires "ABORTED+IRQ within K1+8", `cg_abort` steps_done bins stop at {NSTEPS-1} (no DONE-wins bin), and F-21's abort-at-accept checker asserts "ABORTED, STEPS_DONE ≤ 1" — wrong whenever NSTEPS=1 (first boundary is the final step → DONE). Randomised abort offsets will eventually hit the final boundary and the checker will flag correct behaviour, or the seed set avoids it and the arc ships unverified. |
| 7 | must | §3 env plan, §2 F-16 | The env plan claims the shared agent is a "cocotbext-axi master **+ monitor**" and feeds the scoreboard "by TLM analysis ports from the AXI monitor (writes → stimulus mirror, reads → actual values)". `hw/common/tb/axi_lite_agent.py` contains no monitor and no analysis port — its only classes are `AxiLiteSeqItem`, `AxiLiteDriver`, `AxiLiteAgent` (driver+sequencer), and cocotbext-axi's `AxiLiteMaster` is a BFM, not an "independent protocol agent" with "protocol assertions" as F-16's checker claims (PRD-F16 requires independent protocol checking). Every scoreboard mirror path and the F-16 "0 AXI protocol violations" evidence depend on a monitor that must be written; the plan presents it as already provided. |
| 8 | should | §2 F-05 checker | "per-body ‖r_hw−r_gold‖/… ≤ 2e-9, **same for v**" — PRD-F5 bounds are 2e-9 for r and **5e-11 for v**. As written the plan accepts a v deviation 40× looser than the PRD (masked only by the false "expect 0" of finding 1). |
| 9 | should | §2 F-07 checker | PRD-F7's acceptance is "all fields round-trip through the register interface (0 mismatches)", i.e. idle read-back of pending DT/NSTEPS/NPAIRS/BODY/PAIR (MAS §4: "while idle a read returns what software last wrote"). F-07's checker only mirrors writes into the golden model; no listed test reads pending config back while idle at grape_pipeline top level (test_regs 11/11 is cited only under F-16, at the regs-block unit level, and is not traced to F-07). |
| 10 | should | §2 F-17 checker | PRD-F17 says rejection raises "sticky ERR_PARAM only — no BUSY, no DONE/ABORTED, **no IRQ**", but MAS §8's rejected-doorbell row puts "ERR_PARAM" in the "irq (if enabled)" column (irq = \|(STATUS & IRQ_EN) fires when IRQ_EN[9]=1). The inputs contradict each other; the testplan's F-17 checker is silent on irq, so it checks neither reading. The plan must pick the MAS behaviour (or escalate the conflict) and add an explicit irq expectation with IRQ_EN[9] enabled. |
| 11 | should | §2 F-11 `cg_abort` steps_done bins | The steps_done {0} bin is unreachable: ABORT is sampled only in COMMIT (uArch §3.1/§8), S_ABORT is entered only from S_COMMIT where steps has already incremented to ≥ 1 (`grape_step_fsm.sv:78-83`), and NSTEPS=0 takes LATCH→DONE_S without ABORTED. ABORTED with STEPS_DONE=0 cannot occur; the bin blocks §5's "every bin hit ≥ 1" goal. |
| 12 | should | §2 F-20 test column | F-20's tests are "union of `test_smoke`/`test_corner`/`test_abort`", but `cg_fsm` declares the "reject arc" (IDLE→IDLE on doorbell & ERR_PARAM) as a bin — reject stimulus lives only in `test_errors::param_reject` (F-17). The listed tests cannot hit the declared bin; either add test_errors to the row or move the bin. |
| 13 | should | §2 F-01/F-08 `cg_cfg` | Two bin problems: (a) the NSTEPS {…, 20000} × NPAIRS {1..10} cross demands 20000-step runs at NPAIRS 1..9, which no test generates (`test_full_benchmark` is the benchmark system, NPAIRS=10; `seq_rand_cfg` follows PRD §7 test_random NSTEPS ≤ 64) — ~9 unhittable (or 22-M-cycle-costly) cross bins with no waiver; (b) F-08 cites a "`cg_cfg` NSTEPS=0 bin" but F-01 defines the NSTEPS bins as {1,2,3..10,20000} — no 0 bin exists. |
| 14 | should | §2 F-30 | (a) The derived bin {≤128, **>128**} can only be hit by a run that fails the F-30 checker — a failure-only bin contradicts §5 "every cg_* bin hit ≥ 1". (b) Applying "CYCLES/NSTEPS ≤ 128" to `test_smoke` (NSTEPS=1..2) folds the accept/LATCH/DONE_S overhead (~3 cycles, `grape_step_fsm.sv`) into a single step: worst corner 127 + overhead > 128 fails the checker while K1 — defined at NSTEPS=20000 (PRD §2) — passes. State the bound per-KPI (amortised at 20000) and report the small-N figure. |
| 15 | should | §2 F-12 `cg_irq` | "each STATUS bit × IRQ_EN {0,1}" contains unhittable bins: bit 16 FP_DENORMAL is architecturally constant 0 (MAS §4 "reads 0 until then", uArch §10 "STATUS bit 16 stays 0") and bit 0 BUSY is RO/not in the IRQ_EN 16:1 mask. Restrict to the seven settable sticky bits {DONE, ABORTED, ERR_BUSY, ERR_PARAM, FP_INVALID, FP_DIVZERO, FP_OVERFLOW, FP_UNDERFLOW}. |
| 16 | should | §2 F-16 `cg_axi` BRESP-hold bins | "doorbell BRESP hold {0..4}" bins a quantity the TB cannot control: the hold is DUT-determined (deterministic decode + ERR_PARAM combinational over latched pairs, uArch §3.2), so it takes one or two fixed values; and hold 0 is impossible by construction — MAS §5 returns BRESP "only after the module has latched the configuration and STATUS shows BUSY or ERR_PARAM". Most bins can never be hit. Replace with a single measured-value check ≤ 4. |
| 17 | should | §2 F-11 checker, §4 abort replay | "STEPS_DONE == golden steps completed" is circular: the golden side replays `advance(dt, steps_done_hw, …)`, so it cannot independently predict steps completed — the replay verifies state-consistency-with-the-reported-count, not the count. A firmware-visible off-by-one (RTL commits k steps, reports k, but should have stopped at k−1) passes. The independent check is derivable and omitted: the schedule is static (123/127 cycles per step, uArch §7), so expected steps_done follows from the cycle-stamped abort-write offset; add that bound (±1 step) to the checker. |
| 18 | should | §1/§2 vs PRD §7, MAS §5 | PRD §7 says its acceptance-test names are "reused by hw-dv-testplan", and maps F7–F12/F14–F16 to `test_driver`; the testplan renames these into test_errors/test_irq/test_reset/test_bitexact with no mapping table, and drops `test_driver`'s one unique deliverable: MAS §5 "≈ 4 cycles per transaction … `test_driver` measures the real figure with `cocotbext-axi`" (input to PRD K3's speed-up model). No matrix row measures bus cycles per AXI-Lite transaction. Add the measurement (any test can host it) and a PRD-name→testplan-name mapping in §7. |
| 19 | should | §2 F-02 `cg_fp_class` vs §3 env | `cg_fp_class` samples "operand class … per unit input, from monitor", but §3's only monitors are the AXI agent and `GrapeStatusMonitor` (irq, STATUS snoop, FSM arc watch) — nothing observes the fp64_add/mul/sqrt/rcp operand buses. The covergroup needs a per-unit datapath probe via DUT hierarchy that the env plan does not declare (unlike the FSM watch, which it does). |
| 20 | nit | §2 F-15 | Test list resets in {IDLE, RUN, COMMIT, DONE} but `cg_reset` bins include `latch` — S_LATCH lasts exactly 1 cycle after doorbell accept, so hitting it needs cycle-precise reset timing the test description doesn't provide; and the bin list omits ABORT_S (reset during the 1-cycle abort exit). |
| 21 | nit | §2 F-31 | The row is named "all_done accounting (`retired == acc_total + 30`)" — the equation exists (`rtl/grape_accum.sv:358`) — but the checker is only a 200-cycle per-step watchdog, which catches hangs, not accounting. (Early/miscounted all_done is caught indirectly by bit-exactness; say so, or check the equation via hierarchy.) |
| 22 | nit | §2 F-16 `cg_axi` "unaligned" bin | MAS defines no unaligned-access semantics (the register bus decodes `addr[11:2]`, MAS §4.1, so addr[1:0] silently aliases to the word); the bin has no defined expected behaviour to check against. Either specify the aliasing as the expectation or drop the bin. |

### Summary (pass 1)
22 findings: 7 must, 12 should, 3 nit. Verified true (no finding): `--assert` is armed (`hw/common/scripts/cocotb_run.py:32`); the claimed SVAs exist (issue-slot collision `rtl/grape_pipeline.sv:178-180`, ≤1 accumulate retire `rtl/grape_accum.sv:349`); SCHED_STEP_CYCLES=123 (`rtl/grape_sched_rom.svh:5`); unit TBs 4/4 each and test_regs 11 tests exist (`tb/unit/`); `fp_helpers.ref_op` exists; "CYCLES ≤ 4" for NSTEPS=0 matches PRD-F8 and is feasible (accept+LATCH+DONE_S ≤ 4); the abort-replay golden strategy (replay steps_done_hw full steps) is sound for **state and flags** because ABORT is sampled only at COMMIT so only whole steps ever execute — its residual holes are findings 6 and 17.

## Pass 2 (2026-09-05, agent)

Scope: verify the 7 pass-1 must resolutions against PRD/MAS/uArch/golden sources, re-triage the
12 shoulds, and one fresh full-plan scan for new must-severity problems.

### Verification of pass-1 musts

| pass-1 must # | resolved | evidence |
|---|---|---|
| 1 (F-04/F-05 vs wrong reference) | **yes** | F-04 checker is now two-reference: bit-exact vs `emulation.advance`, THEN \|ΔE/E\| ≤ 1e-12 vs `nbody_ref`; F-05 likewise vs `nbody_ref` with "exact-0 only vs emulation"; §4 tolerance policy spells out both references and the sign-off input (`benchmark_system()`, `advance(0.01, 20000)`). Cited measured values match the sources: ~1.7e-14 (PRD-F4 "1.7e-14 at completion", ~50x vs 1e-12) and ~1.06e-10 (PRD-F5 "maxima 1.06e-10", ~19x vs 2e-9); `golden/calibrate.py` docstring confirms both bounds/margins. Residual: the v bound carried over is wrong — escalated as new finding N1 (was should 8). |
| 2 (F-10 snapshot equality on live counters) | **yes** | F-10 checker now restricts committed-snapshot equality to the BODY window and treats CYCLES/STEPS_DONE as live per MAS §4 ("STATUS and the counters are live", STEPS_DONE 0x048 "live"): monotonic non-decreasing while BUSY, STEPS_DONE ≤ NSTEPS. Consistent with PRD-F10 (state window only). Nit: a strict 64-bit monotonicity compare of live CYCLES_LO/HI reads has no atomicity guarantee (MAS §4 LO/HI note), but a backwards-looking tear needs a LO wrap (> 2³² busy cycles) which no planned run reaches in simulation. |
| 3 (F-13 flag compare ill-defined across runs) | **yes** | §4 now defines the sticky-flag mirror: union of every completed run's `advance` flag set, cleared only on an observed matching W1C or reset on the AXI stream; F-13's checker references it; `emulation.advance` verified to return the per-call flag set (`golden/emulation.py:21-22`). F-33 chained runs are covered by the union semantics. Residual edge case (mid-run W1C) filed as new finding N3. |
| 4 (NPAIRS=0 unverified) | **yes** | `test_corner::npairs0` added (F-32 test column, "NPAIRS=0: integrate-only step"), `cg_cfg` NPAIRS bins now 0..10 (F-01 cross "{0,1..10}", F-32 "bins 0..10 each"), checker asserts positions still integrate and golden agrees — matches PRD-F6 "NPAIRS ∈ [0,10]" and PRD §7 `test_corner` "NPAIRS = 0". |
| 5 (NSTEPS=2³²−1 abort missing) | **yes** | `test_abort::huge_nsteps` (NSTEPS=2³²−1, abort after a few steps, "exercises full counter width") in F-11's test column; `cg_abort` NSTEPS=2³²−1 bin and `cg_cfg` NSTEPS bin "2³²−1(aborted)" added. Matches PRD §7 `test_corner` intent (PRD hosts it under test_corner — naming mapping is still should 18). |
| 6 (DONE-wins on final-boundary abort) | **yes** | F-11 checker now reads "ABORTED **or DONE if the boundary reached is step NSTEPS** (MAS §8 DONE-wins)", test column adds "incl. during final step", `cg_abort` gains a DONE-wins bin; F-21 abort-at-accept checker adds "or DONE when NSTEPS=1 (DONE-wins) — both NSTEPS=1 and NSTEPS>1 driven". Consistent with PRD-F11, MAS §8 ABORT row, and RTL (`rtl/grape_step_fsm.sv` COMMIT: `abort_pend && (steps+1) != nsteps` → S_ABORT, "F11: DONE wins on the final step"). Bonus: the same edit adds the cycle-stamped independent STEPS_DONE cross-check, resolving should 17 (residual: N2). |
| 7 (nonexistent AXI monitor presented as provided) | **yes** | §3 env plan now states the shared agent is "today driver+sequencer only — the passive monitor with TLM analysis ports is a `dv_bringup` deliverable, added to the shared agent so huffman/mtf inherit it". Verified: `hw/common/tb/axi_lite_agent.py` still contains only `AxiLiteSeqItem`/`AxiLiteDriver`/`AxiLiteAgent`. PRD-F16 independence is now explained as cocotbext-axi's own handshake/response checking plus the new monitor's protocol assertions, independent of the driver path — an honest, buildable plan rather than a false claim. |

### Pass-1 shoulds — status

- 8 — **escalated to must** (see N1): F-05 still says "same for v" (2e-9); with the exact-0 mask removed by must-1's fix, the 40x-too-loose v bound is now the operative sign-off check.
- 9 — **open**: F-07 still has no idle read-back round-trip test at grape_pipeline top level.
- 10 — **open**: F-17 checker still silent on irq; PRD-F17 "no IRQ" vs MAS §8 ERR_PARAM-in-irq-column conflict unaddressed.
- 11 — **open**: `cg_abort` steps_done {0} bin still declared and still unreachable (ABORT sampled only in COMMIT, steps ≥ 1 there).
- 12 — **open**: F-20 test column still excludes `test_errors`, so the declared `cg_fsm` reject arc has no stimulus in the listed union.
- 13 — **half-moot**: (b) resolved — F-01 now defines the NSTEPS {0} bin F-08 cites. (a) open and widened: the full NSTEPS × NPAIRS cross now also includes 2³²−1 × NPAIRS {0..10} and 20000 × NPAIRS {0..9} — more unhittable-or-costly cross bins with no waiver noted (§5 waiver mechanism exists but the row doesn't invoke it).
- 14 — **open**: F-30 unchanged — failure-only {>128} bin, and CYCLES/NSTEPS ≤ 128 still applied to test_smoke small-N runs where per-step overhead folding can breach it.
- 15 — **open**: `cg_irq` still "each STATUS bit × IRQ_EN {0,1}" incl. unhittable FP_DENORMAL (bit 16 constant 0) and BUSY (not in IRQ_EN mask).
- 16 — **open**: `cg_axi` doorbell BRESP hold {0..4} bins unchanged (DUT-determined value, hold 0 impossible per MAS §5).
- 17 — **moot**: F-11 now cross-checks STEPS_DONE against the cycle-stamped abort time via the schedule model, independent of the DUT (residual tolerance issue: N2).
- 18 — **open**: no PRD-name→testplan-name mapping table, and bus-cycles-per-AXI-transaction (MAS §5 "test_driver measures the real figure") still unmeasured by any row.
- 19 — **open**: `cg_fp_class` still samples per-unit operand classes "from monitor" but §3 declares no datapath probe on the fp64 unit input buses.

Open shoulds after pass 2: **10** (9, 10, 11, 12, 13a, 14, 15, 16, 18, 19); moot: 17, 13b; escalated: 8 → N1.

### New findings (pass 2)

| # | Severity (must/should/nit) | Location | Finding |
|---|---|---|---|
| N1 | must | §2 F-05 checker, §4 tolerance policy | The v tolerance is wrong everywhere it appears, and — with must-1's exact-0 mask now removed — it is the operative sign-off bound. F-05: "≤ 2e-9 vs `nbody_ref` …, **same for v**"; §4: "PRD-F4/F5 tolerances (\|ΔE/E\| ≤ 1e-12, trajectory 2e-9)" with no v figure. PRD-F5 requires **5e-11 for v** (calibrated max 2.64e-12, ≈19x margin); `golden/calibrate.py` docstring: "F5 = 2e-9 (r) / 5e-11 (v)". As written the plan signs off hardware whose v deviation is up to 40x beyond the PRD bound. 5e-11 (and the 2.64e-12 measured value) appear nowhere in testplan.md. Fix F-05's checker and §4's policy line. (Escalation of pass-1 should 8.) |
| N2 | should | §2 F-11 checker | The new independent STEPS_DONE cross-check is stated as "via the **123-cycle** schedule model", but per-step cycles are data-dependent: uArch §7 gives 123 nominal / **127 worst corner** (sqrt 32 / rcp 24), and RUN→COMMIT waits on retirement, so real step length varies 123..127 with operand data. A fixed-123 prediction drifts up to 4 cycles/step — an abort k steps in mispredicts by up to ⌈4k/123⌉ steps and a strict compare fails correct RTL on corner-latency data. State the check as a window (abort_cycle/127 … abort_cycle/123, ±1 step) or use the measured per-run rate. |
| N3 | should | §4 sticky-flag mirror, MAS §4 | The mirror policy is undefined for W1C **while BUSY**: MAS §4 makes STATUS (W1C) writable while BUSY, but the mirror adds a completed run's whole `advance` flag set at completion — a mid-run W1C clears HW flags raised before it while the mirror later re-adds them from the run's full set, giving a false mismatch on correct RTL. No planned test drives a mid-run FP W1C today, but `test_random`/`seq_w1c` could. Either forbid FP W1C while BUSY in the sequences or define the mirror's mid-run-W1C semantics. |
| N4 | should | §2 F-01 test column vs new bins | The bins added by the must-4/5 fixes are declared in F-01's `cg_cfg` but its test column (`test_smoke`, `test_random`) cannot hit them: NSTEPS=0 stimulus lives in `test_corner::nsteps0`, NSTEPS=2³²−1 in `test_abort::huge_nsteps`, NPAIRS=0 in `test_corner::npairs0` (same pattern as pass-1 finding 12). Fine if `cg_cfg` is env-level and sampled in every test, but then say so; otherwise add the hosting tests to the row. Also feeds the widened cross of should 13a. |

### Summary
**1 must open** (N1 — F-05/§4 v tolerance 2e-9 instead of PRD-F5's 5e-11). All 7 pass-1 musts verified resolved against PRD/MAS/uArch/RTL/golden sources. 10 pass-1 shoulds remain open (9, 10, 11, 12, 13a, 14, 15, 16, 18, 19), 2 moot (13b, 17), 1 escalated (8 → N1); 3 new shoulds (N2, N3, N4).

## Resolutions after pass 2 (author)

- N1 (must): F-05 and §4 now carry the split PRD-F5 bounds — r ≤ 2e-9 (measured ~1.06e-10),
  v ≤ 5e-11 (measured ~2.64e-12). Resolved.
- N2 (should): F-11's STEPS_DONE cross-check uses the 123..127 window, exact after F-30. Resolved.
- N3 (should): §4 defines the mid-run-W1C degradation (subset check until run end). Resolved.
- N4 (should): F-01 notes cg_cfg samples env-wide; extreme bins owned by the corner/abort/
  benchmark tests. Resolved.
- Open shoulds from pass 1 (9, 10, 11, 12, 13a, 14, 15, 16, 18, 19) are accepted as
  bring-up/sign-off refinements; none blocks the testplan gate.

### Summary: 0 must open
