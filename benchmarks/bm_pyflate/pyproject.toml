[project]
name = "pyperformance_bm_pyflate"
requires-python = ">=3.8"
dependencies = ["pyperf"]
urls = {repository = "https://github.com/python/pyperformance"}
version = "1.14.0"

[tool.pyperformance]
name = "pyflate"
